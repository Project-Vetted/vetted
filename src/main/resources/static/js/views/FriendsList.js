

export default function FriendsList(props) {
    //language=HTML
    return `
        <header>
            <div style="margin-top: 80px">
                
                
                
                <h1>Welcome, ${props.user.username}</h1>
            </div>
        </header>
        <main>
            <h1>Your friends</h1>
            <hr>
            <div>${props.user.}</div> 
            
        </main>
    `
}